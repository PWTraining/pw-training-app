// Default location used whenever browser geolocation is denied or
// unavailable, so the header always shows real current conditions rather
// than an error state. Mornington, VIC — Paul's base.
export const FALLBACK_LOCATION = {
  latitude: -38.2167,
  longitude: 145.0333,
};
