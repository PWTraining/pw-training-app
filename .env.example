NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# service role / secret key is server-only — never NEXT_PUBLIC_, never committed.
# add it directly as a Vercel env var and to .env.local (gitignored), not here.
SUPABASE_SECRET_KEY=
